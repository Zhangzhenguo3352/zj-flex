```
自己写的 flex 组件

index.css 
index.min.css

1,	引入
2， 打开浏览器，对照着写	
```

```
还有一个专门打包css 的 webpack 文件，可以借鉴下
	npm install   package.json 需要注意 改下 github路径
	npm run dev   压缩的 css   需要拿出后 再执行 下一个打包
	npm run build 不压缩的 css 
```